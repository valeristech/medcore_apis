-- Baseline: el esquema ya existía en la base (introspección / creación previa).
-- No se aplica ningún cambio. Marca esta migración con:
--   npx prisma migrate resolve --applied 0_baseline
-- Luego ejecuta `npx prisma migrate deploy` para aplicar migraciones posteriores (p. ej. refresh_token).

SELECT 1;
