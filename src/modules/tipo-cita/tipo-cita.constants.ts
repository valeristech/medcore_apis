/** Duración permitida para `tipo_cita.duracion_minutos` (alineado con validación al crear citas). */
export const TIPO_CITA_DURACION_MIN = 5;
export const TIPO_CITA_DURACION_MAX = 480;

/** Formato hex corto (#RGB) o largo (#RRGGBB). */
export const TIPO_CITA_COLOR_RE = /^#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})$/;
